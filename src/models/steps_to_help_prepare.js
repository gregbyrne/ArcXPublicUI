export default class steps_to_help_prepare {
  constructor(id, name, sub_title, description) {
    this.id = id;
    this.name = name;
    this.sub_title = sub_title;
    this.description = description;
  }
}
