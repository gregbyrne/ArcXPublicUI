
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arcx-public-ui-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: arcx-public-ui-web

  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate

  template:
    metadata:
      labels:
        app: arcx-public-ui-web
    spec:
      containers:
      - name: web
        image: $CI_REGISTRY_IMAGE/public-ui-web:$CI_COMMIT_TAG
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: arcx-public-ui-configmap
        env:
        - name: REDEPLOY_META
          value: $CI_JOB_ID-$CI_COMMIT_SHA
        - name: WEB_HTTPS
          value: "false"
        - name: WEB_HTTPS_ONLY
          value: "false"
      imagePullSecrets:
      - name: glcr-auth

---

apiVersion: v1
kind: Service
metadata:
  name: arcx-public-ui-web-service
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 80
    name: https

  selector:
    app: arcx-public-ui-web

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arcx-public-ui-web-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    app.gitlab.com/app: ${CI_PROJECT_PATH_SLUG}
    app.gitlab.com/env: ${CI_ENVIRONMENT_SLUG}
    nginx.ingress.kubernetes.io/auth-tls-secret: ${KUBE_NAMESPACE}/arcx-tls-cert
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/preserve-trailing-slash: "true"
    nginx.ingress.kubernetes.io/ssl-ciphers: "ECDH+AESGCM:ECDH+CHACHA20:DH+AESGCM"
    nginx.ingress.kubernetes.io/proxy-ssl-ciphers: "ECDH+AESGCM:ECDH+CHACHA20:DH+AESGCM"
    nginx.ingress.kubernetes.io/ssl-prefer-server-ciphers: "false"
    nginx.ingress.kubernetes.io/proxy-ssl-protocols: "SSLv2 SSLv3 TLSv1.1 TLSv1.2 TLSv1.3"

spec:
  rules:
  - host: arcx.devsecops-prod.aws.epa.gov
    http:
      paths:
      - path: /
        pathType: Prefix    
        backend:
          service: 
            name: arcx-public-ui-web-service
            port : 
              number: 443
  - host: climateadaptation.epa.gov
    http:
      paths:
      - path: /
        pathType: Prefix    
        backend:
          service: 
            name: arcx-public-ui-web-service
            port : 
              number: 443
  tls:
  - hosts:
    - arcx.devsecops-prod.aws.epa.gov
    - climateadaptation.epa.gov
    secretName: arcx-tls-cert

